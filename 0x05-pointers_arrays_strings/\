#include "main.h"
/**
  *  print_rev -  prints a string, in reverse
  * @s: parameter
  * Return: 0
  */
void print_rev(char *s)
{
	char *a = s;

	while (*a != '\0')
	{
		a++;
	}
	a--;
	while (a >= s)
	{
		_putchar(*a);
		a--;
	}
	_putchar('\n');
}
